#ifndef _ROLLER_SNAPSHOT_SCENES_H
#define _ROLLER_SNAPSHOT_SCENES_H

// Renders the configured named snapshot scene. Returns 0 on success, 1 on
// unknown scene name or scene setup/render failure.
int SnapshotRunScene(void);

#endif
