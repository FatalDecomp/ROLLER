[tools]
cmake = "3.30.2"
ninja = "1.12.0"
python = "3"
rumdl = "0.2.50"
"ubi:zigtools/zls" = "0.15.0"
usage = "latest"
uv = "0.12.2"
zig = "0.15.2"

[env]
EMSDK = "{{config_root}}/.tools/emsdk"
EM_CONFIG = "{{config_root}}/.tools/emsdk/.emscripten"
_.path = [
  "{{config_root}}/.tools/emsdk",
  "{{config_root}}/.tools/emsdk/upstream/emscripten",
  "{{config_root}}/.tools/emsdk/upstream/bin",
]

[hooks]
postinstall = { task = "install-emsdk" }

[tasks.build]
description = "Build for specific platform"
run = "python scripts/build.py"

[tasks.clean]
description = "Clean build artifacts"
run = "rm -rf zig-out .zig-cache"

[tasks.test]
description = "Run tests"
run = "zig build test"

[tasks.format-markdown]
description = "Format architecture Markdown"
run = """
uvx --from mdformat==1.0.0 \
  --with mdformat-gfm==1.0.0 \
  --with mdformat-frontmatter==2.1.2 \
  mdformat CONTEXT.md docs/adr/0002-canonical-state-machines.md
"""

[tasks.lint-markdown]
description = "Lint architecture Markdown"
run = "rumdl check ."

[tasks.install-emsdk]
description = "Install the pinned Emscripten SDK"
run = "python scripts/install_emsdk.py"
